<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!--一个Item就是一层
        top:指定这一边到控件这一面的间距
        0dp：表示和原来一样
        正数：就是边框变小
        负数：边框显示到控件外面（从而达到隐藏效果）
-->
    <item>
        <!--在一个Item中定义一个shape
        可以定义多个-->
        <shape>
            <corners android:radius="@dimen/padding_small" />

            <!--填充颜色为透明-->
            <solid android:color="@color/transparent" />

            <!--边框为灰色-->
            <stroke android:width="@dimen/divider_small" android:color="@color/colorPrimary" />
        </shape>
    </item>

</selector>